import react, { useState } from "react";
import styles from "./currency-card.module.scss";

const CurrencyCard = () => {
  return (
    <div className={styles.container}>
      <div className="confirm">+添加加密货币钱包</div>
    </div>
  );
};

export default CurrencyCard;
